# Deepgram Voice Agent API — required.
DEEPGRAM_API_KEY="your-deepgram-api-key"

# Optional: only needed if you switch the `SPEAK_PROVIDER` in
# `src/live_slides_agent/agent/deepgram_config.py` to ElevenLabs.
ELEVENLABS_API_KEY="your-elevenlabs-api-key"
